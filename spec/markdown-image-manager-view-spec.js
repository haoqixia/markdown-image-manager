'use babel';

import MarkdownImageManagerView from '../lib/markdown-image-manager-view';

describe('MarkdownImageManagerView', () => {
  it('has one valid test', () => {
    expect('life').toBe('easy');
  });
});
